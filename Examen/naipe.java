package Examen;
//Naipe sería una clase que genera el constructor del juego21
//El juego21 seria el main donde cargaremos los objetos
public class naipe {
    //Atributos del naipe
    private int [] naipe = new int [52]; //Tenemos un array de 52 cartas
    
}

int [] arreglo = new int [12]; //arreglo de 12 elementos

//Inicialización
int [] arreglo; //variable del arreglo como array de ints

arreglo = new int[12]; //se inicializa el array con 12 elementos y se le asigna a la variable

arreglo.length // retornaría 12 porque el array arreglo tiene longitud de 12

int [] n = {10, 20, 30, 40};

//n[1] = 20
//n[2] = 30
//n[3] = 40
//n.length = 4

Scanner input = new Scanner(System.in);
int numero1 = input.nextInt();
int numero2 = input.nextInt();


Scanner input = new Scanner(System.in);
int numero3 = input.nextInt();
int numero4 = input.nextInt();

Scanner input = new Scanner(System.in);
int [] numeros = input.nextInt()

int suma;
suma = numeros[0] + numeros[1] + numeros[2] + numeros[3] + numeros [4]





import java.util.Scanner;

Scanner input = new Scanner(System.in);

int [] numeros = new int[5]; //inicializamos el array

System.out.println("Ingrese los numeros");

for(int i = 0; i < 5; i++) //contador
{
    numeros [i] = input.nextInt(); //rellenamos el array
}


int suma = 0;

for( i = 0; i < 5; i++)
{
    suma = suma + numeros[i];
}

input.close();
}

